"# Реализация панели фильтров (Filter Panel)\n\nЭтот план описывает создание плавающего окна фильтров с двумя вкладками: фильтрация таблиц и подсветка столбцов.\n\n## Предлагаемые изменения\n\n### 1. Архитектура состояния (в `DBVisualizer`)\nДобавим новые состояния для управления видимостью и подсветкой:\n- `hiddenTables: Set<string>` — содержит имена таблиц, которые были отключены.\n- `highlightedColumns: Set<string>` — содержит имена столбцов, выбранных для подсветки.\n\nВ `useMemo` для подготовки узлов (`nodes`) и граней (`edges`):\n- Узлы, чье имя (`table.name`) находится в `hiddenTables`, получат свойство `hidden: true`.\n- Грани, у которых `source` или `target` попадает в `hiddenTables`, также получат `hidden: true`.\n- В `data` узлов будет передан массив/сет `highlightedColumns`, чтобы `TableNode` знал, что подсвечивать.\n\n### 2. Новый компонент `FilterPanel.tsx`\nСоздадим перетаскиваемую или плавающую панель (в левом верхнем углу).\nПанель будет содержать:\n- Переключатель вкладок: **Таблицы** / **Столбцы** (с плавным изменением высоты контейнера).\n- Строку поиска для обеих вкладок.\n- **Вкладка Таблицы**: \n  - Кнопки \"Показать все\" и \"Скрыть все\".\n  - Список таблиц (с чекбоксами). Снятие чекбокса добавляет таблицу в `hiddenTables`.\n- **Вкладка Столбцы**:\n  - У
<truncated 1918 bytes>